plugins {
    id 'java'
    id 'application'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
}

test {
    useJUnitPlatform()
}

application {
    mainClassName = 'csvparser.Main'
}

run {
    if (project.hasProperty('arg')) {
        args = [project.getProperty('arg')]
    } else {
        println "[!] Параметр -Parg не передан. Используется по умолчанию: input.txt"
        args = ['input.txt']
    }
}
